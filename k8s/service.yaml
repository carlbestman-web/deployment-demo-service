apiVersion: v1
kind: Service
metadata:
  name: deployment-demo-service
spec:
  type: NodePort
  selector:
    app: deployment-demo-service
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080